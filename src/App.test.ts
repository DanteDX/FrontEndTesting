import React from 'react';
import { shallow } from 'enzyme';

describe('First set of tests', () => {
  test('First First Test', () => {
    expect(1).toBe(1)
  })

  test('First Second Test', () => {
    expect(2).toBe(2)
  });

  test('First Third Test', () => {
    expect(3).toBe(3)
  })
})